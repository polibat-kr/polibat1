version: '3.8'

services:
  # PostgreSQL Database - Windows 로컬 사용으로 비활성화
  # 필요시 아래 주석을 해제하여 Docker PostgreSQL 재활성화 가능
  # postgres:
  #   image: postgres:15-alpine
  #   container_name: polibat-postgres
  #   restart: unless-stopped
  #   ports:
  #     - "5432:5432"
  #   environment:
  #     POSTGRES_USER: polibat
  #     POSTGRES_PASSWORD: polibat_dev_password
  #     POSTGRES_DB: polibat_dev
  #     POSTGRES_INITDB_ARGS: "--encoding=UTF8"
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data
  #     - ./scripts/init-db.sql:/docker-entrypoint-initdb.d/init-db.sql
  #   healthcheck:
  #     test: ["CMD-SHELL", "pg_isready -U polibat"]
  #     interval: 10s
  #     timeout: 5s
  #     retries: 5

  # Redis Cache
  redis:
    image: redis:7-alpine
    container_name: polibat-redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    command: redis-server --requirepass polibat_redis_password
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "--raw", "incr", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  # postgres_data: (Windows 로컬 PostgreSQL 사용으로 비활성화)
  redis_data:
    driver: local

networks:
  default:
    name: polibat-network
